The Business data - create data is adding data through the created webpages NOT the django admin 
site as uposed to Michele's Create Data, which is through the django admin site to add map data.

The map tests are testing the user type-specific map checkboxes. 